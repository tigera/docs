{% assign support_list = site.support_matrix_list | join: ',' | downcase | split: ',' | sort_natural %}
{% assign not_supported = page.not_supported | join: ',' | downcase | split: ','  %}

<div>
    <div className='panel support_list'>      
          <div id="support_matrix_popover" className='panel-head' style={{ fontWeight: '600' }} data-trigger="hover" data-content="These are the currently supported platforms for this feature. Platforms not listed below are unsupported in this version of the product." data-placement="left" onMouseOver="$('#support_matrix_popover').popover('show');">Supported platforms
            <i className='fa fa-question-circle support_list_info'></i>
          </div>
          <div className='panel-body'>
            {% for item in support_list  %}
            {% if page.not_supported != nil and not_supported contains item %}
                {% continue %}
            {% endif %}
            <span className='badge bg-primary'>{{ item }}</span>
            {% endfor %}
        </div>          
      </div>
</div>
