/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  // Use the specific name you provided
  calicoCloudFreeSidebar: [
    // List the document IDs directly as strings in the array.
    'overview',
    'quickstart',
    'connect-cluster-free',
    'disconnect-cluster-free',
  ],
};

module.exports = sidebars;