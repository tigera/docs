{% assign platforms = site.data.support_matrix[page.feature_name]["platforms"] %}

{% assign support_list = platforms | join: ',' | split: ',' | sort_natural %}

{% if support_list.size > 0 %}
<div>
    <div class="panel support_list">      
          <div id="support_matrix_popover" class="panel-head" style="font-weight: 600;" data-trigger="hover" data-content="These are the currently supported platforms for this feature in this release." data-placement="left" onMouseOver="$('#support_matrix_popover').popover('show');">Supported platforms
            <i class="fa fa-question-circle support_list_info"></i>
          </div>
          <div class="panel-body">
            {% for item in support_list  %}
            <span class="badge bg-primary">{{ item }}</span>
            {% endfor %}
        </div>          
      </div>
</div>
{% endif %}
