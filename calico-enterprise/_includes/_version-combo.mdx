{% comment %} Ignore this check here because this file is included twice, once for desktop and once for mobile layout. This trips htmlproofer {% endcomment %}
<button class="btn btn-default btn-block dropdown-toggle" type="button" id="nav-version-dropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" data-proofer-ignore="">
    {% if site.data.versions.first.title == "master" %}nightly{% else %}{% if site.data.versions.first.prerelease %}Preview {% endif %}{{site.data.versions.first.title | regex_replace: site.version_pattern,"Version \1" }}{% endif %}
    <span class="caret"></span>
</button>
<ul class="dropdown-menu" aria-labelledby="nav-version-dropdown" style="margin:0">
</ul>
