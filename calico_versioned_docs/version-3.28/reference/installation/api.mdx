---
description: Installation API reference
---

import variables from '@site/calico_versioned_docs/version-3.28/variables';

# Installation reference

import API from '@site/calico_versioned_docs/version-3.28/reference/installation/_api.mdx';

The Kubernetes resources below configure {variables.prodname} installation when using the operator. Each resource is responsible for installing and configuring a different subsystem of {variables.prodname} during installation. Most options can be modified on a running cluster using `kubectl`.

<API />
