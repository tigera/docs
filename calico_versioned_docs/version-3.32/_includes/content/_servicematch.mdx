A ServiceMatch matches a service in an EntityRule.

| Field     | Description              | Schema |
| --------- | ------------------------ | ------ |
| name      | The service's name.      | string |
| namespace | The service's namespace. | string |

Service matches are only supported when using the Kubernetes datastore driver.
They are not supported by the etcd datastore driver.
